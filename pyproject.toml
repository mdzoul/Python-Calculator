[tool.poetry]
name = "repl_nix_Python-Calculator"
version = "0.1.0"
description = ""
authors = ["mdzoul <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
replit = "^3.2.4"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
